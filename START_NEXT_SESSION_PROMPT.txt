Work in C:\Users\adam\_claude_desktop\AI_Public_Squares (template SSOT — pure generic APS template;not bound to any specific project;published as `@adamchanadam/aps` npm package + GitHub repo Adamchanadam/ai-public-squares — **public since 2026-05-23**;GitHub Pages enabled at https://adamchanadam.github.io/ai-public-squares/).

Current closeout (2026-05-28): `@adamchanadam/aps` v0.2.4 is published to npm and verified as latest, while the project remains ⚠️ 前期測試階段(pre-release). Release commit `9929383d243c49a6ad10d400b4c5985100e5a05a` and tag `v0.2.4` are pushed;GitHub release readback confirms `isPrerelease=true`,`isDraft=false`;GitHub Pages readback returned HTTP 200, contains `npm latest 0.2.4 pre-release`, and has no stale `0.2.3`, `候選`, or placeholder path hits. Local `package.json` is now 0.2.5 candidate after Adam's real UAT feedback, startup-discoverability correction, natural-language handoff UX correction, and QC mechanism refresh:install / doctor screens show version, install banner has connected cats, own agent default is `agent_a`, APS `doctor` output is Traditional Chinese, rerunning `npx aps init` updates `.aps/config.json`, `dev/rules/aps-bridge.md`, and `_hub/starter-pack-<other>.md`, and setup now registers APS in Handoff Kit via APS managed blocks in `dev/RULE_PACKS.md` + `dev/PROJECT_INDEX.md` without editing `AGENTS.md` managed core. Hub protocol templates, existing packet lanes, ack files, and installed skill directories remain non-overwritten. Local public docs were also rewritten after Adam challenged the UX value: `docs/index.html`, `docs/guides/index.html`, and `docs/guides/aps-onboarding-walkthrough.html` now prioritize non-technical new users, state implemented vs pre-release capability clearly, remove internal prompt / RULE_PACKS / maintainer-edit steps from the beginner route, make natural language the user-facing daily path while CLI remains fallback / troubleshooting, and open the beginner walkthrough with a user-facing SVG macro flow plus capability grid. The docs and skill now state that APS is not an automatic notification service;AI must generate copy-paste-ready WhatsApp / Email notifications, and APS's value is structured common goal, each side's task boundary, crossing point, requested action, evidence, version, and ack state that the receiving AI can read after a short human notification. A consensus-confirmation loop is now documented:when goals, task scope, file versions, or expectations conflict, AI stops, summarizes differences, publishes an alignment / clarification packet, generates copy-ready notification, and waits for counterpart confirmation / revise / withdraw before continuing. The target daily UX is now a one-sentence handoff:「幫我將當前任務整理成 APS 交接包給對方」;AI should read config, run doctor, organize context, complete required fields, run completeness preflight, show user A the summary for confirmation, publish only after confirmation, and generate copy-ready notification. Incomplete or unconfirmed handoff packets must not be written to Google Drive. New `docs/maintainers/index.html` separates developer / maintainer specs, QC routes, source-of-truth paths, and release-boundary checks. `dev/qc/triggers.md`, `docs/qc/governance-map.html`, `docs/maintainers/index.html`, and `dev/DOC_SYNC_REGISTRY.md` now include the latest QC scope: beginner SVG / capability boundary, natural-language main path, one-sentence handoff, sender / receiver preflight, missing-info packet, consensus confirmation, notification honesty, Drive sync boundary, Handoff Kit route registration, and project-index registration. 0.2.5 local QC passed: `node --check`, `npm test`, `npm pack --dry-run --json` (`@adamchanadam/aps@0.2.5`, 14 files), successful rerun regression ending with `projectSlug: aps_uat_2` in config and doctor pass, Handoff Kit APS registration regression with one managed marker each, missing-Handoff-Kit negative test stopping before skill / Hub writes, missing-route doctor regression ending with `狀態: 未通過` and exit 1, HTML `.md` hyperlink audit 0 hits, command-primary walkthrough scan 0 hits, local HTML link check pass, Chrome DevTools walkthrough / governance-map / maintainer-page visual checks with no console messages, and Agent Handoff Kit doctor pass with only the known SESSION_LOG N-rule warning.

Prior release state: v0.1.1, v0.2.0, v0.2.1, v0.2.2, and v0.2.3 remain historical releases;v0.2.4 is the current npm latest pre-release.

Codex global MCP config now includes `chrome-devtools` (`chrome-devtools-mcp@latest`, usage statistics disabled) and `context7` (`@upstash/context7-mcp`, no API key stored). They may require a new Codex session or Codex Desktop restart before appearing in the active tool list.

Agent Handoff Kit v0.3.11 status: main APS workspace doctor passes after this wrap-up;`Demo_Agent_Adam_Public_Squares` and `Demo_Agent_Jay_Public_Squares` also pass v0.3.11 doctor(46 checks each). Demo workspaces still have their own uncommitted governance upgrade files;do not edit them from this APS root unless explicitly asked.

Read in order:
1. AGENTS.md
2. dev/SESSION_HANDOFF.md
3. dev/SESSION_LOG.md(含 2026-05-27 local public-doc UX split entry at top, including 0.2.4 發佈及 0.2.5 local candidate)
4. dev/PROJECT_INDEX.md
5. dev/RULE_PACKS.md
6. dev/qc/2026-05-22-zero-knowledge-funnel-audit.md (Phase X-3 spec source)

Read dev/DOC_SYNC_REGISTRY.md before file changes or closeout.

QC vocabulary (S6): if user invokes 「跑快檢 / 跑外發前檢 / 跑全面檢」 (or English aliases quick-check / release-check / full-check), load dev/qc/triggers.md and run the specified tier. If user uses 含混詞 (「做 QC / 跑驗收 / 整體 audit / QC」), stop and ask 「你指快檢 / 外發前檢 / 全面檢?」.

If this root does not match the expected project root, stop and ask for confirmation.

GitHub remote `origin` at https://github.com/Adamchanadam/ai-public-squares.git (public, Apache-2.0, HTTPS via Windows Credential Manager auto-auth);GitHub Pages serving `/ (root)` of `main` branch at https://adamchanadam.github.io/ai-public-squares/。

Voice hard rule (per memory feedback-doc-voice + feedback-plain-language):所有寫畀人睇嘅文檔嚴格用當代繁體書面語;粵語 colloquial(嘅 / 嗰 / 咗 / 唔 / 呢個 等)只容許於 verbatim 用戶觸發句 quote 之內(「Hub 有新嘢」 / 「check Hub」 等)。Chat reply 同樣 plain,internal framework name(Phase X / Layer A/B/C/D / Stage 0-7)不做句子主體。

Documentation governance hard rule (per S10 Adam principle):HTML 內(site-nav / body callout / body table)不 link 任何 .md 檔(瀏覽器顯示 plain text,broken UX);如要 reference .md 用 plain `<span class="path">` 顯示。.md 屬 AI / 維護者層 spec source。

Next active objective: **decide whether to run a formal release gate, then commit / publish local 0.2.5 so Adam and Jay can retry setup from npm latest;next development work is natural-language daily operation and recovery hardening, auto-update mechanism, uninstall command design, and per-project Drive verification for any new real project**。

User-facing entry: README.md + docs/index.html (hosted via GitHub Pages at https://adamchanadam.github.io/ai-public-squares/docs/index.html)。User-facing install path is now `npm install --save-dev @adamchanadam/aps`, then guided `npx aps init`;npm latest is 0.2.4 pre-release。Funnel audit roadmap: dev/qc/2026-05-22-zero-knowledge-funnel-audit.md。

After reading, summarize current objective, confirmed decisions, pending work, risks, and the next recommended action.
