# AI Public Squares v0.2.1 — Pre-release

This is a pre-release hardening update for the APS CLI and bundled skill.

## Added

- `npx aps revise` to publish the next immutable version of a packet.
- `npx aps withdraw` to withdraw the latest unconsumed packet version.
- `npx aps doctor` to run a read-only Hub skeleton, ack, outbox, and conflict-filename check.
- Clearer CLI next-step guidance after publish, revise, withdraw, consume, close, and doctor.

## Clarified

- README, public docs, walkthrough, and skill wording distinguish npm latest 0.2.0 from this 0.2.1 pre-release.
- Natural-language daily operation and recovery remain pre-release hardening work.
- Each real project still needs its own Google Drive path and sync verification.

## Verification

- Formal release-check passed.
- Formal full-check passed and is recorded in `dev/qc/2026-05-27-aps-full-audit.md`.
- Local disposable Hub regression covered init, publish, inbox, revise, withdraw, consume, close, doctor, and negative paths.
- npm package dry-run confirms 14 expected files.
