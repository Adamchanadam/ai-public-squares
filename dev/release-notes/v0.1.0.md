# AI Public Squares v0.1.0

AI Public Squares 的首個 npm 試行版。此版本已被 v0.1.1 取代;v0.1.1 將公開文檔與 CLI help 改為已驗證的安裝方式。

## 本版包含

- `bridge-pack` 命令，可輸出 User A / User B 兩種 Bridge Pack fixture。
- npm package 打包範圍包含 `bin/`、`skills/`、`examples/`、`README.md`、`LICENSE`。
- README 與 GitHub Pages 公開文檔已標明目前邊界：`bridge-pack` 可用，`init` 仍為佔位功能。
- 全面檢已補齊本地 HTML 預覽、臨時 Hub 往返回歸、Bridge Pack 啟動行為記錄。

## 仍未完成

- `npx aps init` 尚未完成一鍵設置。
- 真實跨機運作仍待後續試行。
- 不建議用於正式生產或不可中斷的重要流程。
