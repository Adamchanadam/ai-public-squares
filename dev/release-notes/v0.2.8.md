# AI Public Squares v0.2.8 — GitHub pre-release

Post-UAT Reliable Pair hardening release.

## Status

- GitHub source pre-release.
- npm latest remains `@adamchanadam/aps@0.2.7` until a separate `npm publish` is explicitly authorized and completed.

## Fixed

- Role B guided setup now defaults to `agent_b / agent_a`, preventing the second machine from accidentally accepting the same `agent_a / agent_b` identity pair as Role A.
- APS packet notifications now lead with a readable summary, attention notes, and the human-in-loop `check Hub` next step.
- Receiver-side guidance now emphasizes local alignment checks before consuming or starting work.

## Updated

- Documented `cmd /c claude -p "<bounded read-only review prompt>"` as an optional local external-review command in the project index.
- Aligned public docs, skill wording, maintainer docs, QC map, and roadmap around summary-style human notification and receiver-side local alignment checks.
- Added a post-UAT full-check report recording the remaining release blockers.

## Verification

- Agent Handoff Kit doctor: 46 checks passed, with SESSION_LOG N-rule closeout warning only.
- `node --check bin\aps.js`
- `node bin\aps.js --help`
- `npm test` (placeholder-only)
- `npm pack --dry-run --json`
- `git diff --check`
- Local HTML link scan and Chrome DevTools preview
- Isolated two-workspace APS round-trip: `init`, `doctor`, `publish --body-file`, `inbox`, `consume`, `revise --body-file`, reply, `close`, `withdraw`, final no-pending inbox, and `upgrade`

