# Doc Sync Registry

Purpose: map change types to documents and checks. Keep this as rows, not long prose.

## Status Vocabulary

Use: `confirmed`, `unverified`, `pending`, `blocked`, `not_applicable`.

| Change type | Also check/update | Verification |
|---|---|---|
| New file or directory | `dev/PROJECT_INDEX.md` Directory Map | path listed |
| Stack or command change | `dev/PROJECT_INDEX.md` Stack / Entry Points | command verified or marked unverified |
| Public behavior change | README, public docs, changelog | docs mention current behavior |
| API or SDK behavior change | API docs, examples, tests | tests or documented reason |
| Runbook change | runbook path in `PROJECT_INDEX.md` | procedure still executable |
| Governance rule change | relevant pack/core, registry, README if public-facing | complexity budget checked |
| Closeout/startup contract change | `AGENTS.md`, `dev/SESSION_HANDOFF.md`, `dev/SESSION_LOG.md`, `dev/PROJECT_INDEX.md`, README quick usage | opening message schema + workspace identity present |
| Workspace identity change | `dev/SESSION_HANDOFF.md`, `dev/PROJECT_INDEX.md` | root/branch/commit/status recorded or marked unverified |
| Release | release notes, README version, changelog, public docs, `dev/SESSION_HANDOFF.md`, `dev/SESSION_LOG.md`, `dev/PROJECT_INDEX.md` | release pack checklist; after external publish, read back registry / release body / live public page / remote branch and tag before claiming completion |
| APS protocol / plan / verification change | `docs/plans/2026-05-*.md` (design / MVP plan / MVP verification / Phase 4 plan), `docs/index.html` (status badge / Phase 4 status line), `G:\我的雲端硬碟\Adam 工作目錄\AI_Projects\AI_Public_Squares\_hub\PROTOCOL.md` + `_hub\CHANGELOG.md`, both demo workspaces' `dev/rules/aps-bridge.md` | doctor green; cross-doc consistency check across the four APS docs (including Phase 4 plan if touched); PROTOCOL.md CHANGELOG sign-off; template version-anchor comments still match PROTOCOL version |
| APS public product roadmap / Reliable Pair direction | `docs/plans/2026-05-28-aps-public-product-multi-agent-roadmap.md`, `dev/PROJECT_INDEX.md`, README / public docs / maintainer docs / QC docs / skill / CLI help if any user-facing promise changes | roadmap exists and is indexed; recent product direction is Reliable Pair first; Reliable Pair uses `.aps/config.json` `otherAgentId` and does not require Contacts selector; summary-style human notification and receiver-side `check Hub` report / local alignment check are treated as Reliable Pair UX, while Contacts selector, file-based notify / watch / Telegram bot auto-send / automatic AI triggering remain deferred or opt-in future work; public surfaces only promise current two-person APS capabilities and do not present unbuilt routes as supported; core path does not require extra cloud developer projects, external service credentials, or secret keys |
| APS round-trip session (live packet, ack, or close on the Drive Hub) | local `dev/SESSION_LOG.md` Sync field with `APS publish/consume/close: <packet_id> v<N>` line; `ack.json` in the agent's own lane | grep `^- \*\*Sync:\*\* APS` in SESSION_LOG returns ≥ 1 new line; `_ack/<me>.ack.json` `consumed[]` reflects any inbound packet acted on |
| APS user-facing docs change (`docs/index.html`, `docs/guides/**.html`) | the affected public HTML files plus `dev/PROJECT_INDEX.md` Fact Base rows for any added/removed page; public `.site-nav` stays limited to user-facing entry paths (`入口`, `教學中心`); maintainer-only HTML lives outside the new-user route and may have its own maintainer nav | manual link audit (index ↔ guides hub ↔ each walkthrough); public nav does not route new users into QC / maintainer pages; Fact Base row exists for each guide or maintainer HTML file |
| Repo entry layer change (Layer A — `README.md`, `docs/index.html`, install command wording, product name) | `README.md` + `docs/index.html` 的痛點 hook / install command / 點開始步驟 / Build status / deep-dive reference list 之間的 narrative consistency;public site-nav brand text (`<span class="site-nav__brand">...`) 跨 public docs HTML 一致;product name(`AI Public Squares` 對齊 README + repo + npm scope);funnel audit doc 的 Build status 與 README / docs/index.html 的 phase / status wording 同步 | grep `npm install --save-dev @adamchanadam/aps@latest` 出現於 README + docs/index.html;grep `site-nav__brand">` value 於 public docs HTML 一致;funnel audit `dev/qc/2026-05-22-zero-knowledge-funnel-audit.md` 的 phase status 與 README / docs/index.html status wording 不矛盾 |
| npm package change (`package.json`, `bin/aps.js`, version bump, `npm publish`) | `package.json` version / bin path / dependencies / files 含 `bin/` `skills/` `examples/` `resources/`;`bin/aps.js` 各 sub-command 處理路徑(`init --dry-run` / `init --target claude --dry-run` / `init --target codex --dry-run` / `init --hub-root ...` / `upgrade` / `upgrade --dry-run` / `config` / `publish` / `publish --body-file` / `revise` / `revise --body-file` / `inbox` / `consume` / `withdraw` / `close` / `doctor` / `bridge-pack` / `--help` / unknown);README install command + Build status table 反映 latest version;GitHub release / tag 對應 npm version;post-publish state sync updates release notes, GitHub release body, README / public docs wording, handoff, log, and project index | smoke test:跑 `node bin/aps.js` 各路徑(`--help` / `init --dry-run` / `init --target claude --dry-run` / `init --target codex --dry-run` / `init --hub-root <tmp> --project <slug> --agent-id <id> --other-agent-id <id> --role A` / `upgrade --dry-run` and `upgrade` against existing config / verifies `dev/rules/aps-bridge.md` + `.aps/config.json` + APS managed route in `dev/RULE_PACKS.md` + APS managed block in `dev/PROJECT_INDEX.md` / `config --dry-run` / `config` / disposable Hub config-short command flow `publish --body-file` → `inbox` → `revise --body-file` → `inbox` → `withdraw` → `inbox` → `publish` → `consume` → `close` → `doctor` / `bridge-pack` / `bridge-pack --role B` / `bogus` / invalid target / invalid setup params)— 正常路徑 exit 0,`bogus` 與 invalid paths exit 1;`doctor` 必須檢查 Handoff Kit APS route + project-index registration;`bridge-pack` output 為 valid markdown;`package.json` valid JSON + `npm pack --dry-run` 出現預期 file list 含 `examples/demo-agent-{a,b}/dev/rules/aps-bridge.md`,`skills/aps/references/setup-dialogue.md`,`resources/protocol/PROTOCOL.md`,`resources/protocol/templates/*`;README install command 同 `package.json` name 一致;publish 後必讀回 `npm view @adamchanadam/aps version dist-tags.latest bin dist.fileCount --json`、`npx --yes @adamchanadam/aps@latest --help`、GitHub release body、GitHub Pages、remote branch / tag,且結果須寫入治理狀態同步提交 |
| HTML 公開面 .md hyperlink strip(維護者層 .md 不上公開 nav) | `docs/**.html` site-nav / body 不得 link 任何本 repo `docs/plans/**.md` 或 `dev/**/*.md`(瀏覽器開出 plain text,broken UX);如要 reference .md,strip hyperlink 留 plain `<span class="path">` 顯示;public nav 不把新手導入 QC / maintainer page,maintainer page 可連其他 HTML 參考頁但仍不可 link 本地 .md | `grep -rn 'href=.*\\.md' docs/` 預期僅命中外部 GitHub link;public HTML nav audit confirms new-user pages only expose 入口 / 教學中心;README 分兩層 sub-list 完整 |
| Skill source change(`skills/aps/SKILL.md`、skill 觸發詞 / 子流程 orchestration / voice 規矩 / supporting files) | Frontmatter `description` ≤1024 字符且與 `when_to_use`(如使用)合計 ≤1,536(skill listing cap);body voice 規矩段對應 Adam memory `feedback-doc-voice` + `feedback-plain-language`;觸發短語集合對齊 funnel audit `dev/qc/2026-05-22-zero-knowledge-funnel-audit.md` §3 階段 3-5 + §4 Layer C 表(短語涵蓋 APS 簡寫 + AI Public Squares 正式名 + Agent Public Squares 自然語言別名 + 痛點性語句 + 粵語 / 英文 mixed);子流程設計對齊 funnel audit §4 + 跨機接駁計劃 `docs/plans/2026-05-21-aps-phase4-plan.md` T0-T10 + 設置教學 `docs/guides/aps-onboarding-walkthrough.html` §3-§10;dialogue carve-out 文件(`docs/plans/2026-05-23-aps-skill-dialogue-script.md`)對齊 funnel audit §6;APS 與 Agent Handoff Kit 版本 / banner 分流、agent_id Hub 共享身份一致性、發包前內容 + topic + 寫入確認、長正文 `--body-file` 安全輸入必須同步;`package.json` `files` array 將來需含 `skills/`(交叉引用「npm package change」row 的 files spec) | grep `^name: aps` 命中 × 1;觸發短語每個 intent group(初次設置 / 發佈 / 收件 / 補救)各至少 1 字面短語 + 1 語意備用句出現於 frontmatter description 與 body Section 3;`教我用 APS` / `教我用 AI Public Squares` / `教我用 Agent Public Squares` 三條入口須同時出現於 skill source 與 QC 規則;`agent_id` 一致性規則以 skill source 為準,CLI / starter pack / README / walkthrough 只可作短鏡像或執行驗證;body 內子流程 heading(`## 4` / `## 5` / `## 6` / `## 7`)命中各 ≥ 1;grep `品牌與版本分流硬規則`、`--body-file`、`確認三件事` 命中 skill source;voice 抽驗(人工)— 任選 3 段 body prose,句句獨立讀都自足、無內部編號做主語、無中英「半通不通」片段;frontmatter `name` `description` 兩欄齊備 |
| APS product consistency audit / public promise consistency | `README.md` + `docs/index.html` + `docs/guides/index.html` + `docs/guides/aps-onboarding-walkthrough.html` + `docs/maintainers/index.html` + `docs/qc/governance-map.html` + `dev/qc/triggers.md` + `bin/aps.js` + `skills/aps/SKILL.md` + dialogue script + Bridge Pack fixtures + `resources/protocol/*`;確認「目前可用」「使用邊界」「手動替代路徑」「npm publish 狀態」不互相混淆;全面檢須覆蓋公開承諾一致性、發佈前可信度、協定實際運行正確性,並按零認知讀者入口、手動設置、日常協作、出錯補救四條主要使用流程走通;最新流程必須包括新手 SVG 宏觀理解 / 能力清單、一語交接、發送前預檢、摘要式通知、收件總覽、收件後預檢、本機對接檢查、補交需求、共識確認、通知邊界與 Drive 同步邊界 | `dev/qc/*aps-full-consistency-audit.md` 有最新結論;maintainer HTML states 三條主線 + 四條流程 + 最新橫切保障 and public HTML does not force new users through maintainer specs;grep `private repo\|私人 repo` 於 README + docs 0 命中;grep `34 / 34\|34/34` 於 walkthrough 0 命中;公開面如寫 `aps init` 已支援技能安裝 / Hub skeleton 或 CLI 最小往返,必須同時標明自然語言日常操作、補救流程與真實跨機 Google Drive 往返仍未完成;若 `npm view @adamchanadam/aps ...` 顯示 registry latest 未含本地變更,公開面須標明 release-prep 邊界或延後 publish |
| APS full audit report (`dev/qc/<date>-aps-full-audit.md`) | `dev/PROJECT_INDEX.md` Entry Points + Fact Base;`dev/SESSION_HANDOFF.md` Validation / QC + Next Priorities;`dev/SESSION_LOG.md` latest entry;`START_NEXT_SESSION_PROMPT.txt` if active objective changes | report exists, is indexed, states 三條主線 + 四條主要使用流程 + 18 項檢查結果, and is committed before claiming 🔴 全面檢 passed |

## Registry Rule

If a change has no matching row, add a row before closeout or record why no durable sync rule is needed. At closeout, record sync status for every row touched by the session.
