# Doc Sync Registry

Purpose: map change types to documents and checks. Keep this as rows, not long prose.

## Status Vocabulary

Use: `confirmed`, `unverified`, `pending`, `blocked`, `not_applicable`.

| Change type | Also check/update | Verification |
|---|---|---|
| New file or directory | `dev/PROJECT_INDEX.md` Directory Map | path listed |
| Stack or command change | `dev/PROJECT_INDEX.md` Stack / Entry Points | command verified or marked unverified |
| Public behavior change | README, public docs, changelog | docs mention current behavior |
| API or SDK behavior change | API docs, examples, tests | tests or documented reason |
| Runbook change | runbook path in `PROJECT_INDEX.md` | procedure still executable |
| Governance rule change | relevant pack/core, registry, README if public-facing | complexity budget checked |
| Closeout/startup contract change | `AGENTS.md`, `dev/SESSION_HANDOFF.md`, `dev/SESSION_LOG.md`, `dev/PROJECT_INDEX.md`, README quick usage | opening message schema + workspace identity present |
| Workspace identity change | `dev/SESSION_HANDOFF.md`, `dev/PROJECT_INDEX.md` | root/branch/commit/status recorded or marked unverified |
| Release | release notes, README version, changelog | release pack checklist |
| APS protocol / plan / verification change | `docs/plans/2026-05-*.md` (design / MVP plan / MVP verification / Phase 4 plan), `docs/index.html` (status badge / Phase 4 status line), `G:\我的雲端硬碟\Adam 工作目錄\AI_Projects\AI_Public_Squares\_hub\PROTOCOL.md` + `_hub\CHANGELOG.md`, both demo workspaces' `dev/rules/aps-bridge.md` | doctor green; cross-doc consistency check across the four APS docs (including Phase 4 plan if touched); PROTOCOL.md CHANGELOG sign-off; template version-anchor comments still match PROTOCOL version |
| APS round-trip session (live packet, ack, or close on the Drive Hub) | local `dev/SESSION_LOG.md` Sync field with `APS publish/consume/close: <packet_id> v<N>` line; `ack.json` in the agent's own lane | grep `^- \*\*Sync:\*\* APS` in SESSION_LOG returns ≥ 1 new line; `_ack/<me>.ack.json` `consumed[]` reflects any inbound packet acted on |
| APS user-facing docs change (`docs/index.html`, `docs/guides/**.html`) | the affected HTML files in `docs/` plus `dev/PROJECT_INDEX.md` Fact Base rows for any added/removed page; the inline `.site-nav` block must stay in sync across all HTML pages under `docs/` | manual link audit (index ↔ guides hub ↔ each walkthrough), site-nav `<a>` set identical across pages, Fact Base row exists for each guide HTML file |
| Repo entry layer change (Layer A — `README.md`, `docs/index.html`, install command wording, product name) | `README.md` + `docs/index.html` 的痛點 hook / install command / 點開始步驟 / Build status / deep-dive reference list 之間的 narrative consistency;site-nav brand text (`<span class="site-nav__brand">...`) 跨 4 個 docs/ HTML 一致;product name(`AI Public Squares` 對齊 README + repo + npm scope);funnel audit doc 的 Build status 與 README / docs/index.html 的 Build status table 同步 | grep `npx @adamchanadam/aps init` 出現於 README + docs/index.html;grep `site-nav__brand">` value 於 4 個 docs HTML 一致;funnel audit `dev/qc/2026-05-22-zero-knowledge-funnel-audit.md` 的 phase status 與 README Build status table row 一一對應 |
| npm package change (`package.json`, `bin/aps.js`, version bump, `npm publish`) | `package.json` version / bin path / dependencies / files 含 `bin/` `skills/` `examples/` `resources/`;`bin/aps.js` 各 sub-command 處理路徑(`init --dry-run` / `init --target claude --dry-run` / `init --target codex --dry-run` / `init --hub-root ...` / `publish` / `inbox` / `consume` / `close` / `bridge-pack` / `--help` / unknown);README install command + Build status table 反映 latest version;GitHub release / tag 對應 npm version | smoke test:跑 `node bin/aps.js` 各路徑(`--help` / `init --dry-run` / `init --target claude --dry-run` / `init --target codex --dry-run` / `init --hub-root <tmp> --project <slug> --agent-id <id> --other-agent-id <id> --role A` / disposable Hub `publish` → `inbox` → `consume` → reply → `close` / `bridge-pack` / `bridge-pack --role B` / `bogus` / invalid target / invalid setup params)— 正常路徑 exit 0,`bogus` 與 invalid paths exit 1;`bridge-pack` output 為 valid markdown;`package.json` valid JSON + `npm pack --dry-run` 出現預期 file list 含 `examples/demo-agent-{a,b}/dev/rules/aps-bridge.md`,`skills/aps/references/setup-dialogue.md`,`resources/protocol/PROTOCOL.md`,`resources/protocol/templates/*`;README install command 同 `package.json` name 一致 |
| HTML 公開面 .md hyperlink strip(維護者層 .md 不上公開 nav) | 4 個 `docs/**.html` site-nav 不得 link 任何 `docs/plans/**.md` 或 `dev/**/*.md`(瀏覽器開出 plain text,broken UX);`docs/index.html` body table + walkthrough 之 callout / 附錄表如要 reference .md,strip hyperlink 留 plain `<span class="path">` 顯示;`README.md` 「想深入了解」 list 跨 HTML 人類面 vs .md AI / 維護者層 分層 + 加 disclaimer | `grep -rn 'href=.*\.md' docs/` 預期僅命中外部 GitHub link 或本檔自身;4 個 HTML site-nav `<a>` 集合一致(對齊 row「APS user-facing docs change」之 site-nav 一致規矩);README 分兩層 sub-list 完整 |
| Skill source change(`skills/aps/SKILL.md`、skill 觸發詞 / 子流程 orchestration / voice 規矩 / supporting files) | Frontmatter `description` ≤1024 字符且與 `when_to_use`(如使用)合計 ≤1,536(skill listing cap);body voice 規矩段對應 Adam memory `feedback-doc-voice` + `feedback-plain-language`;觸發短語集合對齊 funnel audit `dev/qc/2026-05-22-zero-knowledge-funnel-audit.md` §3 階段 3-5 + §4 Layer C 表(短語涵蓋 APS 簡寫 + AI Public Squares 全名 + 痛點性語句 + 粵語 / 英文 mixed);子流程設計對齊 funnel audit §4 + 跨機接駁計劃 `docs/plans/2026-05-21-aps-phase4-plan.md` T0-T10 + 設置教學 `docs/guides/aps-onboarding-walkthrough.html` §3-§10;dialogue carve-out 文件(`docs/plans/2026-05-23-aps-skill-dialogue-script.md`)對齊 funnel audit §6;`package.json` `files` array 將來需含 `skills/`(交叉引用「npm package change」row 的 files spec) | grep `^name: aps` 命中 × 1;觸發短語每個 intent group(初次設置 / 發佈 / 收件 / 補救)各至少 1 字面短語 + 1 語意備用句出現於 frontmatter description 與 body Section 3;body 內子流程 heading(`## 4` / `## 5` / `## 6` / `## 7`)命中各 ≥ 1;voice 抽驗(人工)— 任選 3 段 body prose,句句獨立讀都自足、無內部編號做主語、無中英「半通不通」片段;frontmatter `name` `description` 兩欄齊備 |
| APS product consistency audit / public promise consistency | `README.md` + `docs/index.html` + `docs/guides/index.html` + `docs/guides/aps-onboarding-walkthrough.html` + `docs/qc/governance-map.html` + `dev/qc/triggers.md` + `bin/aps.js` + `skills/aps/SKILL.md` + dialogue script + Bridge Pack fixtures + `resources/protocol/*`;確認「目前可用」「目標體驗」「手動替代路徑」「npm publish 狀態」四者不互相混淆;全面檢須覆蓋公開承諾一致性、發佈前可信度、協定實際運行正確性,並按零認知讀者入口、手動設置、日常協作、出錯補救四條主要使用流程走通 | `dev/qc/*aps-full-consistency-audit.md` 有最新結論;grep `公開承諾一致性\|發佈前可信度\|協定實際運行正確性` 於 `dev/qc/triggers.md` + 4 個入口 HTML 全部命中;grep `零認知讀者入口流程\|手動設置流程\|日常協作流程\|出錯補救流程` 於 `dev/qc/triggers.md` + 4 個入口 HTML 全部命中;grep `private repo\|私人 repo` 於 README + docs 0 命中;grep `34 / 34\|34/34` 於 walkthrough 0 命中;公開面如寫 `aps init` 已支援技能安裝 / Hub skeleton 或 CLI 最小往返,必須同時標明自然語言日常操作、補救流程與真實跨機 Google Drive 往返仍未完成;若 `npm view @adamchanadam/aps ...` 顯示 registry latest 未含本地變更,公開面須標明本地開發版 / 下一版邊界 |

## Registry Rule

If a change has no matching row, add a row before closeout or record why no durable sync rule is needed. At closeout, record sync status for every row touched by the session.
